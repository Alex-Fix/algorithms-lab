{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C Algorithm",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/${fileDirnameBasename}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Algorithms",
            "terminal": "integrated"
        }
    ]
}