# algorightms-lab

A dual-language repository for mastering algorithms. 
**Workflow:** Prototype in Python (Visual) → Implement in C (Performance).

## Setup
1. **System Tools:** `brew install llvm make python`
2. **Python Env:**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
3. **Open in VS Code**: code . (Install recommended extensions when prompted).

## Run C
```bash
make 
./bin/01_sorting/selection_sort
```